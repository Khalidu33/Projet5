{% extends'base.html.twig' %}

{% block title 'Parc MaguyLand' %}

{% block body %}
    <header id="header">
        {% include 'nav/nav.html.twig' %}
        <h1>bhkxjvdkjxvslj hcdjls!cjcbfmdilckhalidu</h1>
    </header>

    <div class="container">
        
        <h1>{{ attraction.title }}</h1>
        <p class="card-text">{{ attraction.age }} ans</p>
        <p class="card-text">{{ attraction.season }}</p>
        <p class="card-text">{{ attraction.description }} ans</p>
        <a href="" class="btn btn.primary">Contacter l'agence</a>
    </div>

{% endblock %}